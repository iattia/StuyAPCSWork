public class GoTile extends Tile {
    public GoTile(String name, int position) {
        super(name, position);
    }
    
    @Override
    public void landOn(Player p) {
        // TODO: Add $200 to player's balance
    }
}